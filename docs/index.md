# Hades Game Engine

This site is built with MkDocs Material and published through GitHub Pages.

Use the architecture pages for:

- a high-level view of core engine/editor modules
- an auto-generated class relationship and usage diagram

## Local docs commands

```bash
python3 -m pip install -r requirements-docs.txt
python3 scripts/generate_class_diagram.py
mkdocs serve
```
